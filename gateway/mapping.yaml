Version: v0.1.3 # cli版本验证，非必要不修改

Mapping:
  # router 生成匹配关键字，不要修改
  APIMatchMapping:
    - "@Group"
    - "@Route"
    - "@Method"
    - "@Middleware"
    - "@Doc"

  APIMiddlewareMapping:
    JWT: "middleware.JWTAuth()"
    # 可自定义

  APIHandleMapping:
    APIHandler:
      - "Create"
      - "List"
      - "Info"
      - "Delete"
      - "Update"
      - "Status"
    # 可自定义添加
