Gateway:
  ServerName: jingxiu-cli-server
  Version: v0.3.3 #当前版本号为模板版本号，可自定义
  Listen: 127.0.0.1:8000

Logger:
  FilePath: "log"
  FileName: "latest"

DB:
  Type:     # 数据库类型
  Version:  # 版本号
  Source:   # 连接地址
Cache:
  Type: ""    # 缓存烈性
  Version: "" # 版本号
  Source:     # 连接地址
Rpc:
  # some Rpc config
Jwt:
  Secret: # jwt 加密盐值
  Expire: # 过期时间
  Unit: # 时间单位 s 秒，m 分，h 时
